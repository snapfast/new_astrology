import { performance } from 'perf_hooks';
import { calculateAllShadBala } from './src/lib/astrology.ts';

// We just need a basic test of performance for shadbala with the modified code vs before.
// Actually, I already ran a simplified benchmark above. Let's just run tests to verify.
