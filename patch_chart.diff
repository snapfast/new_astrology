<<<<<<< SEARCH
import { useState, useEffect, useRef, FormEvent, useMemo, KeyboardEvent } from 'react';
import { useRouter, useSearchParams } from 'next/navigation';
import { useLanguage } from '@/context/LanguageContext';
import { sendGAEvent } from '@next/third-parties/google';
=======
import { useState, useEffect, useRef, FormEvent, useMemo, KeyboardEvent } from 'react';
import { useRouter, useSearchParams } from 'next/navigation';
import { useLanguage } from '@/context/LanguageContext';
import { sendGAEvent } from '@next/third-parties/google';
import { useNominatim, type Suggestion } from '@/hooks/useNominatim';
>>>>>>> REPLACE
<<<<<<< SEARCH
interface Suggestion {
  name: string;
  lat: string;
  lon: string;
}

interface StoredChartData {
=======
export interface StoredChartData {
>>>>>>> REPLACE
<<<<<<< SEARCH
  const searchTimeoutRef = useRef<NodeJS.Timeout | null>(null);
  const abortControllerRef = useRef<AbortController | null>(null);

  // Load history from localStorage on mount
  useEffect(() => {
    try {
      const stored = localStorage.getItem(HOROSCOPE_HISTORY_KEY);
=======
  // Load history from localStorage on mount
  useEffect(() => {
    try {
      const stored = localStorage.getItem(HOROSCOPE_HISTORY_KEY);
>>>>>>> REPLACE
<<<<<<< SEARCH
  useEffect(() => {
    return () => {
      if (searchTimeoutRef.current) clearTimeout(searchTimeoutRef.current);
      if (abortControllerRef.current) abortControllerRef.current.abort();
    };
  }, []);

  const [suggestions, setSuggestions] = useState<Suggestion[]>([]);
  const [isSearching, setIsSearching] = useState(false);
  const [showSuggestions, setShowSuggestions] = useState(false);
=======
  const { suggestions, isSearching, fetchSuggestions, setSuggestions } = useNominatim();
  const [showSuggestions, setShowSuggestions] = useState(false);
>>>>>>> REPLACE
<<<<<<< SEARCH
  const handlePobChange = (e: React.ChangeEvent<HTMLInputElement>) => {
    const val = e.target.value;
    setFormData(prev => ({ ...prev, pob: val, lat: '', lon: '' }));
    setErrors(prev => ({ ...prev, pob: '' }));
    setShowSuggestions(true);
    setHighlightedIndex(-1);

    if (val.length < 2) {
      setSuggestions([]);
      return;
    }

    if (searchTimeoutRef.current) clearTimeout(searchTimeoutRef.current);
    if (abortControllerRef.current) abortControllerRef.current.abort();

    searchTimeoutRef.current = setTimeout(async () => {
      // Check cache first
      if (SUGGESTIONS_CACHE.has(val)) {
        setSuggestions(SUGGESTIONS_CACHE.get(val) || []);
        return;
      }

      setIsSearching(true);
      const controller = new AbortController();
      abortControllerRef.current = controller;

      try {
        const response = await fetch(
          `https://nominatim.openstreetmap.org/search?q=${encodeURIComponent(val)}&format=json&addressdetails=1&limit=5&featuretype=city`,
          { signal: controller.signal }
        );
        const data = await response.json();
        interface NominatimAddress {
          city?: string;
          town?: string;
          village?: string;
          suburb?: string;
          hamlet?: string;
          state?: string;
          country?: string;
        }
        interface NominatimItem {
          address: NominatimAddress;
          display_name: string;
          lat: string;
          lon: string;
        }
        const uniqueCitiesMap = data.reduce((map: Map<string, Suggestion>, item: NominatimItem) => {
          const { address, lat, lon, display_name } = item;
          const city = address.city || address.town || address.village || address.suburb || address.hamlet;
          const { state, country } = address;
          const name = city ? `${city}${state ? `, ${state}` : ''}, ${country}` : display_name;

          map.set(name, { name, lat, lon });
          return map;
        }, new Map<string, Suggestion>());
        const uniqueCities: Suggestion[] = Array.from(uniqueCitiesMap.values());

        // Cache the result to prevent redundant network calls for the same query
        if (SUGGESTIONS_CACHE.size >= MAX_CACHE_SIZE) {
          const firstKey = SUGGESTIONS_CACHE.keys().next().value;
          if (firstKey !== undefined) SUGGESTIONS_CACHE.delete(firstKey);
        }
        SUGGESTIONS_CACHE.set(val, uniqueCities);

        if (!controller.signal.aborted) {
          setSuggestions(uniqueCities);
        }
      } catch (error: any) {
        if (error.name !== 'AbortError') {
          console.error('Error fetching cities:', error);
          if (!controller.signal.aborted) {
            setSuggestions([]);
          }
        }
      } finally {
        if (!controller.signal.aborted) {
          setIsSearching(false);
        }
      }
    }, 500);
  };
=======
  const handlePobChange = (e: React.ChangeEvent<HTMLInputElement>) => {
    const val = e.target.value;
    setFormData(prev => ({ ...prev, pob: val, lat: '', lon: '' }));
    setErrors(prev => ({ ...prev, pob: '' }));
    setShowSuggestions(true);
    setHighlightedIndex(-1);

    fetchSuggestions(val);
  };
>>>>>>> REPLACE
<<<<<<< SEARCH
const SUGGESTIONS_CACHE = new Map<string, Suggestion[]>();
const MAX_CACHE_SIZE = 50;

const ChartGeneration: React.FC<ChartGenerationProps> = ({ className = '', initialValues, isUpdate = false }) => {
=======
const ChartGeneration: React.FC<ChartGenerationProps> = ({ className = '', initialValues, isUpdate = false }) => {
>>>>>>> REPLACE
